&&:champ

# Metadata &?epic &a0
- [ ] Create one struct to represent amp.Item
	- Path
	- Meta
	- Wikilink
	- Support for parsing and completing
- Support using spaces inside amp.Path based on `backtick-wrapping`
- [ ] Replace templates with fixed metadata items
	- `&?what`
		- Supports WBS levels: proj, area, epic, story, task
		- When a named def is present (cfr Node.def), it is assigned to that.
		- When a named def is not present, it goes to the unnamed def.
			- Except for dependency injection, this results in tree-based aggregation.
		- [ ] Use this for reporting Chores etc
		- [ ] Use this as default namespace for resolving.
			- Might require to resolve in 2 passes: defs and whats, rest
	- `&$cost`
		- Summed into Chore for all defs that are referenced
	- `&!prio`
		- Sum of 'max per what'
		- [ ] Use this for Chore order
	- `&@who`
		- Name of assigned worker, the first mentionned in a Node is the one (exclusive)
		- [ ] Support jumping to this worker def
		- [ ] Support listing all Chores assigned to a worker
		- [?] Is this correct?
	- `&date`
	- Metadata should not be distributed down the tree
	- [ ] Remove the exclusive flag if possible: from content and amp path
		- Else, find another method to represent prio metadata
	- [ ] Update mero.Parser to support `&!`, now, `!` terminates an amp path
	- [ ] Remove all use of existing templates in the content
	- [ ] Document that AMP mainly works via distributing tags. Aggregation happens at the Chore level.
		- [?] Can we remove the aggregation phase on Forest?
- [?] Should we support injection of metadata via tag dependency?
	- Makes it less clear where prio settings can happen

# Backlog
- [ ] Use workspace/diagnostics to find todos
	- This is currently bound to `textDocument/implementation` and `textDocument/typeDefinition`
	- Add a specific LSP section in config to narrow this search down if needed: `fui.zon` for now
- [ ] Inherited prio should be overruled and not set at max
- [ ] Rework prio to use `&10a`
	- This supports inter-project shifting better
		- Updating prios could be used for this
	- Only work with relative prios within a project `&a`
- [x] Use `[*]` to indicate that a task is 'ready to go'
- [x] Replace hardcoded 'geertf' home folder with `rubr.fs.Path`
- [/] Support for &&wikilink
	- Disabled for now
	- [?] Support both amp paths and wikilinks in Def via union or extend amp.Path
- [x] Do not allow definitions that match with Prio
	- [?] Maybe also with Date etc?
- [ ] Interpret a lowercase sentence as a todo

# TODO Project &project

## TODO Epic &epic
