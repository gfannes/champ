&&:champ

# Metadata &&meta
- [x] Support Meta in amp.Path first level
	- [x] Update parsing to detect the use of special characters
		- [x] Status is Meta as well
		- [x] Support `TODO` and `[ ]`
	- [x] Support for setting an exclusive assignee via `&^@geertf`
	- [ ] Enforce that all assignees are defined as `&&@geertf`
- [x] Do not aggregate amp.Path with Meta from Node.org to Node.agg
	- [x] Inject metadata into def in forest.resolveAmps()
- [x] Collect amp.Path with Meta in Chore or Def
	- [/] Implement Forest.computeChores()
		- [x] Cost
		- [/] Absolute and relative Prio
			- [?] Maybe keep track of summative and maxative prios? Now, a large prio shift erases the finer prio tuning.
- [ ] Fully document Term, Node, Def and Chore
	- Is there a need for each of these?
- [ ] Update reporting
- [ ] Remove all use of templates
	- [ ] Update prio computation
	- [ ] Support updating all current prios
- [ ] Add e2e tests based on &naft
- [ ] Depend on inherit Path inheritance to reduce amps per Node
- [ ] Use `/` iso `:` as path separator
	- [ ] Support identification of `:` from `champ ch`
- [ ] Create a phony Def for each Node/Chore
- Aggregation is lookup in Defs
	- Decide there how aggregation and inheritance rules work
	- Only non-Meta is distributed down from org to agg
	- Maybe rename agg into inh
- Precompute dependencies per Def?
	- If this is not too memory-hungry: Count Defs
- [ ] Support Wikilinks:
	- `[[abc]]` normal link to page `abc`
	- `&[[abc]]` this Node is part of `abc`
	- `[[abc]]&` this Node depends on `abc`: inject all local Meta into `abc`
- Is there still a usecase for exclusive?
	- If so, use `^` iso `!` since this is XOR in C
- [ ] Support using spaces inside amp.Path based on `backtick-wrapping`
- [x] Replace templates with fixed metadata items
	- `&?what`
		- Supports WBS levels: proj, area, epic, story, task
		- When a named def is present (cfr Node.def), it is assigned to that.
		- When a named def is not present, it goes to the unnamed def.
			- Except for dependency injection, this results in tree-based aggregation.
		- [ ] Use this for reporting Chores etc
		- [ ] Use this as default namespace for resolving.
			- Might require to resolve in 2 passes: defs and whats, rest
	- `&$cost`
		- Summed into Chore for all defs that are referenced
	- `&!prio`
		- Sum of 'max per what'
		- [ ] Use this for Chore order
	- `&@who`
		- Name of assigned worker, the first mentionned in a Node is the one (exclusive)
		- [ ] Support jumping to this worker def
		- [ ] Support listing all Chores assigned to a worker
		- [?] Is this correct?
	- `&date`
	- [ ] Metadata should not be distributed down the tree but injected into a Def
	- [x] Update mero.Parser to support `&!`, now, `!` terminates an amp path
	- [ ] Remove all use of existing templates in the content
	- [ ] Document that AMP mainly works via distributing tags. Aggregation happens at the Chore level.
		- [?] Can we remove the aggregation phase on Forest?

# Backlog
- [ ] Use workspace/diagnostics to find todos
	- This is currently bound to `textDocument/implementation` and `textDocument/typeDefinition`
	- Add a specific LSP section in config to narrow this search down if needed: `fui.zon` for now
- [ ] Inherited prio should be overruled and not set at max
- [ ] Rework prio to use `&10a`
	- This supports inter-project shifting better
		- Updating prios could be used for this
	- Only work with relative prios within a project `&a`
- [x] Use `[*]` to indicate that a task is 'ready to go'
- [x] Replace hardcoded 'geertf' home folder with `rubr.fs.Path`
- [/] Support for &&wikilink
	- Disabled for now
	- [?] Support both amp paths and wikilinks in Def via union or extend amp.Path
- [x] Do not allow definitions that match with Prio
	- [?] Maybe also with Date etc?
- [ ] Interpret a lowercase sentence as a todo

# TODO Project &project

## TODO Epic &epic
